# Roys Legion Milvus local development template

# Use a local runtime directory for Milvus/etcd/MinIO state.
DOCKER_VOLUME_DIRECTORY=./data

# Local-only demo credentials. Override these for any shared or remote deployment.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
